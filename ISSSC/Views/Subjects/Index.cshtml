@model IEnumerable<ISSSC.Models.EnumSubject>

@{
    ViewBag.Title = "Index";
    ViewBag.ActiveMenuItem = "menu-subjects";
}

<div class="body-content container-fluid text-center">
    <div class="row content">
        <div class="col-sm-2 sidenav">
        </div>

        <div class="col-sm-8 text-left">
            <h1>Správa předmětů</h1>
            <p>
                @Html.ActionLink("Přidat předmět", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Code)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Lesson)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Code)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Lesson)
                        </td>
                        <td>
                            @Html.ActionLink("Upravit", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Detaily", "Details", new { id = item.Id }) |
                            @Html.ActionLink("Smazat", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }

            </table>
        </div>
        <div class="col-sm-2 sidenav">
        </div>
    </div>
</div>
